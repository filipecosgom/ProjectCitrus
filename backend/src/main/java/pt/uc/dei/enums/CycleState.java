package pt.uc.dei.enums;

/**
 * Enum representing the status of a cycle.
 * - OPEN: Indicates the cycle is currently active and ongoing.
 * - CLOSED: Indicates the cycle has ended or is no longer active.
 */
public enum CycleState {
    OPEN,
    CLOSED;
}