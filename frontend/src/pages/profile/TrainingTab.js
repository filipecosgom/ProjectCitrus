import React from "react";

export default function TrainingTab({ userId }) {
  // Aqui podes buscar e mostrar dados de formação do utilizador usando o userId, se necessário
  return (
    <div className="training-tab">
      <h2>Formação</h2>
      <p>Conteúdo da formação do utilizador.</p>
      {/* Adiciona aqui a lógica e UI da formação */}
    </div>
  );
}
